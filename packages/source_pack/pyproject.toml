[project]
name = "context-builder-source-pack"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "context-builder-api",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/source_pack"]
