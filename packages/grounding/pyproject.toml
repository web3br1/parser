[project]
name = "context-builder-grounding"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "context-builder-model-gateway",
  "context-builder-normalizers",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/grounding"]
