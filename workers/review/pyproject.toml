[project]
name = "worker-review"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["celery>=5.4.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/worker_review"]
